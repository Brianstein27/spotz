<div
  class="
    no-scrollbar -mx-4 my-4 inline-flex sm:max-xl:w-screen flex-nowrap overflow-x-auto
  "
>
  <div class="ml-4 flex">
    <% events.each do |event| %>
        <%= render "shared/card_event", event: event %>
    <% end %>
  </div>
</div>
